#compdef cfgctl rcup rct rcq rcd

map=$zdotdir/etc/config.map

_cfg_dsts_available=( $( awk '/^cvs_wd/ { print $3 }' $map ) )

local expl
_description cfg_dsts expl 'config packages destinations available'
compadd -a _cfg_dsts_available
