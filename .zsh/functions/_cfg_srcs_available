#autoload

map=$zdotdir/etc/config.map

_cfg_srcs_available=( $( awk '/^cvs_wd/ { print $2 }' $map ) )

local expl
_description cfg_srcs expl 'config package sources available'
compadd -a _cfg_srcs_available
