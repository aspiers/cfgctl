#!/bin/bash

set -e

#normalize-bookmarks

if [ -z "$*" ]; then
  echo "#>>> cvs updating config ..."
  cd ~/.cfg/config
  cvsuhq

  echo "#>>> cvs updating priv ..."
  cd ~/.cfg/priv
  cvsuhq

  echo "#>>> cfgctl ..."
  cfgctl
else
  dirs=
  for pkg in "$@"; do
    cd ~/.cfg/$pkg
    echo "#>>> cvs updating $pkg ..."
    cvsuhq
    echo "#>>> cfgctl $pkg ..."
    cfgctl "$pkg"
  done
fi

