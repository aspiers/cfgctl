#!/bin/bash

set -e

#normalize-bookmarks

root=~/.cvs
if [ -z "$*" ]; then
  dirs="config priv"
else
  dirs=
  for pkg in "$@"; do
    cd ~/.cfg/$pkg
    echo "#>>> cvs updating $pkg ..."
    cvsuhq
    echo "#>>> cfgctl $pkg"
    cfgctl "$pkg"
  done
fi

