#!/bin/bash

set -e

#normalize-bookmarks

if [ -z "$*" ]; then
  echo "#>>> cvs updating config ..."
  cd ~/.cvs/config
  cvsuhq

  echo "#>>> cvs updating priv ..."
  cd ~/.cvs/priv
  cvsuhq

  echo "#>>> cfgctl ..."
  cfgctl
else
  dirs=
  for pkg in "$@"; do
    cd ~/.cfg/$pkg
    echo "#>>> cvs updating $pkg ..."
    cvsuhq
    echo "#>>> cfgctl $pkg ..."
    cfgctl "$pkg"
  done
fi

