#!/bin/bash

set -e

if [ -z "$*" ]; then
  echo "#>>> cvst config ..."
  cd ~/.cvs/config
  cvst

  echo "#>>> cvst personal ..."
  cd ~/.cvs/personal
  cvst
else
  dirs=
  for pkg in "$@"; do
    cd ~/.cfg/$pkg
    echo "#>>> cvst $pkg ..."
    cvst
  done
fi

