#!/bin/bash

set -e

if [ -z "$*" ]; then
  echo "#>>> cvst config ..."
  cd ~/.cvs/config
  cvst

  echo "#>>> cvst priv ..."
  cd ~/.cvs/priv
  cvst
else
  dirs=
  for pkg in "$@"; do
    cd ~/.cfg/$pkg
    echo "#>>> cvst $pkg ..."
    cvst
  done
fi

