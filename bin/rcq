#!/bin/bash

set -e

if [ -z "$*" ]; then
  echo "#>>> cvsq config ..."
  cd ~/.cvs/config
  cvsq

  echo "#>>> cvsq priv ..."
  cd ~/.cvs/priv
  cvsq
else
  dirs=
  for pkg in "$@"; do
    cd ~/.cfg/$pkg
    echo "#>>> cvsq $pkg ..."
    cvsq
  done
fi

